extends layout/main
include mixins/videoBlock

block content
    .home-videos
        each video in videos
            +videoBlock({
                id: video.id,
                fileUrl: video.fileUrl,
                title: video.title,
                description: video.description,
                views: video.views
            })
            +videoBlock({
                id: video.id,
                fileUrl: video.fileUrl,
                title: video.title,
                description: video.description,
                views: video.views
            })
            +videoBlock({
                id: video.id,
                fileUrl: video.fileUrl,
                title: video.title,
                description: video.description,
                views: video.views
            })
            +videoBlock({
                id: video.id,
                fileUrl: video.fileUrl,
                title: video.title,
                description: video.description,
                views: video.views
            })
            